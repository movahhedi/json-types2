{
	"name": "json-types2",
	"version": "1.0.0",
	"description": "JSON TypeScript definitions",
	"main": "index.js",
	"author": "movahhedi",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "https://github.com/movahhedi/json-types2.git"
	},
	"homepage": "https://github.com/movahhedi/json-types2#readme",
	"bugs": {
		"url": "https://github.com/movahhedi/json-types2/issues"
	},
	"keywords": [
		"json",
		"typescript",
		"types",
		"tsd",
		"typings"
	],
	"scripts": {
		"clean": "rimraf --glob ./*.js && rimraf --glob ./*.d.ts",
		"build": "tsc",
		"test": "echo \"Error: no test specified\" && exit 1",
		"prepublish": "npm run clean && npm run build"
	},
	"devDependencies": {
		"rimraf": "^5.0.5",
		"typescript": "^5.4.5"
	}
}
